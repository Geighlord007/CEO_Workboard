# 每周任务控制台 · 国内免费部署指南（邮箱+密码版）

本版本改为 **邮箱 + 密码** 登录（脱离 Google），国内生态即可免费部署。
架构（组合一）：

| 组件 | 用什么 | 费用 | 说明 |
|---|---|---|---|
| 前端+后端（一个容器） | **Zeabur 免费档**（或 Sealos）| 免费 | 用现成 Dockerfile |
| 数据库 | **TiDB Cloud Serverless 新加坡免费档** | 免费 | MySQL 兼容，国内直连快 |
| 登录 | 邮箱 + 密码 | 免费 | 无需任何第三方 |

> 前端本来可以拆成 OSS 静态托管 + 后端单独跑，但那样要改部署方式。
> **本版保持「单容器」最省事**：前端构建产物由后端 Hono 一起托管，一个 Docker 搞定，
> 和原来 Cloud Run 一样。Zeabur 免费档足够跑这个低流量单租户应用。

---

## 准备：所需账号

1. **TiDB Cloud**（免费数据库）→ https://tidbcloud.com/
2. **Zeabur**（免费部署平台）→ https://zeabur.com/zh-CN
3. 一个 **Node.js ≥ 20** 的本地环境（用来生成密钥、跑迁移）

---

## 第 1 步：建 TiDB 免费数据库（约 5 分钟）

1. 打开 https://tidbcloud.com/ ，注册登录；
2. 「Create Cluster」→ 选 **Serverless / Starter**（免费档）：
   - 区域选 **Singapore**（国内访问快）；
   - 名字随便，如 `wtc`；
3. 创建完成后点「Connect」→「Public」连接：
   - 记录 host / port / 用户名，点「Generate Password」生成密码并保存；
4. 拼出连接串（后面填进环境变量 `DATABASE_URL`）：

```
mysql://<用户名>:<密码>@<host>:4000/wtc?ssl={"rejectUnauthorized":true}
```

> `/wtc` 是数据库名，迁移脚本会自动创建。

---

## 第 2 步：填本地配置 + 跑迁移（本地电脑，约 10 分钟）

```bash
cd 项目目录

# 1. 生成会话密钥
openssl rand -hex 32        # 复制输出

# 2. 填配置（邮箱密码 + 数据库）
cp .env.production.example .env.production
# 编辑 .env.production，填入：
#   ADMIN_EMAIL            = 你的邮箱（第一个管理员）
#   ADMIN_INITIAL_PASSWORD = 一个初始密码（首次登录用，之后可改）
#   ALLOWED_EMAILS         = 你和老板邮箱，逗号分隔（第一个是管理员）
#   APP_SECRET             = 刚生成的密钥
#   DATABASE_URL           = 第 1 步拼的 TiDB 连接串

# 3. 建表 + 灌演示数据（把 .env.production 的环境变量读进来）
Set-Content -Path ".env.production" -Value (Get-Content ".env.production")
# Windows PowerShell 下直接把变量导出到当前会话后跑：
Get-Content .env.production | ForEach-Object {
  if ($_ -match '^\s*([A-Z_]+)\s*=\s*(.*)$') {
    [Environment]::SetEnvironmentVariable($matches[1], $matches[2])
  }
}
npm install
npm run db:push
npx tsx db/seed.ts          # 看到"完成：历史任务…"即成功

# Linux/macOS 则是：
#   set -a; source .env.production; set +a
```

---

## 第 3 步：部署到 Zeabur（约 15 分钟）

1. 打开 https://zeabur.com/ ，登录（支持 GitHub / 邮箱）；
2. 「新建项目」→「部署新服务」；
3. **方式 A（推荐，无 Docker 平台限制）**：把本项目推到 GitHub 私有仓库，在 Zeabur 里
   「关联 Git 仓库」选它，Zeabur 检测到 Dockerfile 会自动用 Docker 构建；
   **方式 B**：用「上传/拖拽」或 CLI 部署本地构建的镜像。
4. 服务启动命令默认读 Dockerfile 里的 `CMD ["npm","start"]`，**无需改**；
5. 在服务「变量」里填（对应 .env.production 的内容，逐个添加）：
   ```
   NODE_ENV=production
   APP_SECRET=...
   DATABASE_URL=mysql://...
   ALLOWED_EMAILS=...
   ADMIN_EMAIL=...
   ADMIN_INITIAL_PASSWORD=...
   LLM_BASE_URL=https://api.deepseek.com/v1   (可选)
   LLM_API_KEY=sk-...                          (可选)
   LLM_MODEL=deepseek-chat                     (可选)
   ```
6. 构建完成后，Zeabur 会给你一个 `https://xxxx.zeabur.app` 的网址。

> **重要提醒**：.env.production 里的 `ADMIN_EMAIL` / `ADMIN_INITIAL_PASSWORD` 是为了首次登录，
> 数据库迁移与 Zeabur 的环境变量**保持一致**，否则首次登录会失败。

---

## 第 4 步：首次登录并设置密码（2 分钟）

1. 浏览器打开你的 Zeabur 网址 → 跳到登录页；
2. 输入 **ADMIN_EMAIL** 邮箱 + **ADMIN_INITIAL_PASSWORD** 初始密码 → 登录成功；
3. 登录后这个邮箱的密码哈希已写入数据库，**以后用它自己设的密码登录**。

> 安全建议：首次登录后，把 Zeabur 环境变量里的 `ADMIN_INITIAL_PASSWORD` 删掉/改掉，
> 这样即使泄露初始化密码也无法再登录（数据库里已是新密码）。

---

## 日常更新（以后每次改代码）

```bash
cd 项目目录
./build 并推到 Git 仓库   # 或按 Zeabur 的自动部署
# Zeabur 关联 Git 后，push 即自动重新构建上线；数据在 TiDB，不会丢
```

---

## 常见问题

| 现象 | 原因与处理 |
|---|---|
| 登录页显示"该账号尚未设置密码" | 首次登录要用 ADMIN_EMAIL + ADMIN_INITIAL_PASSWORD；确认 Zeabur 变量与迁移时一致 |
| 提示"邮箱不在名单内" | ALLOWED_EMAILS 里没这个邮箱；改 Zeabur 变量后重新部署 |
| 页面能开但数据空/报错 | DATABASE_URL 错或迁移没跑；检查连接串密码里特殊字符要 URL 编码 |
| 想用 Google 登录 | 可选：在 Zeabur 变量加 GOOGLE_CLIENT_ID 即可同时支持（登录页会多出一个 Google 按钮） |

---

## 安全清单

- [ ] .env.production 不进 git（已在 .gitignore）
- [ ] ADMIN_INITIAL_PASSWORD 首次登录后从部署平台删掉/改掉
- [ ] ALLOWED_EMAILS 只放可信邮箱
- [ ] 老板只拿 /r/<令牌> 只读链接，不给登录权限